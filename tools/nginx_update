#!/bin/bash
set -e

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
export PYTHONPATH="$repo_root/management"

exec /usr/local/lib/mailinabox/env/bin/python3 -c '
from nginx_update import do_nginx_update
from utils import load_environment

print(do_nginx_update(load_environment()), end="")
'

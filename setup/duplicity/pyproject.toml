[project]
name = "s5mail-duplicity"
version = "0.0.0"
description = "Locked, isolated duplicity runtime for S5 Mail"
requires-python = ">=3.14,<3.15"
dependencies = [
	"duplicity==3.1.0",
]

[tool.uv]
package = false
required-version = "==0.11.30"

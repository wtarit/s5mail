# Fail2Ban configuration file for S5 Mail. Do not edit.
# This file is re-generated on updates.

[DEFAULT]
# Whitelist our own IP addresses. 127.0.0.1/8 is the default. But our status checks
# ping services over the public interface so we should whitelist that address of
# ours too. The string is substituted during installation.
ignoreip = 127.0.0.1/8 PUBLIC_IP ::1 PUBLIC_IPV6

[dovecot]
enabled = true
filter  = dovecotimap
logpath = /var/log/mail.log
findtime = 30
maxretry = 20

[s5mail-management]
enabled = true
filter = s5mail-management-daemon
port = http,https
logpath = /var/log/syslog
maxretry = 20
findtime = 30

[s5mail-munin]
enabled  = true
port     = http,https
filter   = s5mail-munin
logpath  = /var/log/nginx/access.log
maxretry = 20
findtime = 30

[s5mail-owncloud]
enabled  = true
port     = http,https
filter   = s5mail-owncloud
logpath  = STORAGE_ROOT/owncloud/nextcloud.log
maxretry = 20
findtime = 120

[s5mail-postfix465]
enabled  = true
port     = 465
filter   = s5mail-postfix-submission
logpath  = /var/log/mail.log
maxretry = 20
findtime = 30

[s5mail-postfix587]
enabled  = true
port     = 587
filter   = s5mail-postfix-submission
logpath  = /var/log/mail.log
maxretry = 20
findtime = 30

[s5mail-roundcube]
enabled  = true
port     = http,https
filter   = s5mail-roundcube
logpath  = /var/log/roundcubemail/errors.log
maxretry = 20
findtime = 30

[recidive]
enabled  = true
maxretry = 10
action   = iptables-allports[name=recidive]
# In the recidive section of jail.conf the action contains:
#
# action   = iptables-allports[name=recidive]
#            sendmail-whois-lines[name=recidive, logpath=/var/log/fail2ban.log]
#
# The last line on the action will sent an email to the configured address. This mail will
# notify the administrator that someone has been repeatedly triggering one of the other jails.
# By default we don't configure this address and no action is required from the admin anyway.
# So the notification is omitted. This will prevent message appearing in the mail.log that mail
# can't be delivered to fail2ban@$HOSTNAME.

[postfix-sasl]
enabled  = true

[sshd]
enabled = true
maxretry = 7
bantime = 3600
